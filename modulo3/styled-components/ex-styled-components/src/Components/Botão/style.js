import styled from "styled-components";

export const BotãoEstilo = styled.button`
    width: 150px;
    border-radius: 5px;

    &:hover {
        cursor: pointer;
    }

    &:active {
        cursor: pointer;
        transform: scale(0.98);
    }
`